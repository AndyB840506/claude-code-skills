# Handoff: CCC Case File No. 001 — recording through publish + quote cards

**Date:** 2026-07-04
**Status:** Complete — episode published, two small open items waiting on Andy

---

## What We Accomplished This Session

- Republished the BTQ EP.020 and CCC Case File No. 001 scripts as Claude Artifacts (original
  URLs weren't saved anywhere, so these are new links); rebuilt CCC's as a styled noir HTML
  page (`CaseFile001-eight-is-great-guion.html`) matching its 1940s pulp visual identity,
  since the first version rendered as plain white markdown.
- Ran the recorded CCC pilot audio through `transcriptor` (English, WhisperX large-v2,
  diarized) — clean run, first English run on this machine (auto-downloaded the English
  wav2vec2 aligner).
- **Found and corrected a real bug in the process, not just the episode:** the script's
  own footer claimed ~6,700 words for a 45-min target; the actual recording ran ~23 min.
  First theory (paraphrased/condensed delivery) was wrong — Andy corrected it. Recounting
  the script's real spoken dialogue (excluding cues/headers/sources) gave only
  ~2,955-3,116 words — the script itself was under-written, never verified before being
  marked complete. Fixed at the source: `podcast-creator/workflows/01-episodio.md` now has
  a mandatory checklist step to programmatically verify a finished script's word count
  before calling it done, plus a pointer to per-show `guion-style-*.md` calibration files.
- Created `corporate-crime-confidential-production/guion-style-ccc.md` — CCC's first
  duration calibration doc (137 wpm measured), mirroring what BTQ already has.
- Generated show notes (`shownotes-ep001.md`) and YouTube metadata (`youtube-ep001.md`)
  sized to the real ~23-min runtime, with timestamps pulled directly from the transcript.
- Reviewed the 3 generated cover-art images against `artwork-ep001.md`'s brief — found two
  concrete issues on the 1:1 (main Spotify cover): under Spotify's 3000×3000 minimum, and
  title text filled crimson instead of the spec'd parchment fill. Both still need a
  regenerate; 9:16 and 16:9 passed.
- Episode published: `https://open.spotify.com/episode/3slIwuP6C38iuRCBuGThu4` — filled the
  real URL into show notes/YouTube metadata placeholders.
- Generated 6 quote-card prompts (`quotecards-ep001.md`). Applied a new rule specific to
  CCC's docu-drama format: real documented quotes get attributed by name (e.g. John
  Stumpf); dramatized first-person lines (composite banker, Tolstedt's reconstructed
  monologue) get attributed to the show, never to the real person, to avoid presenting
  fiction as a verbatim quote. Encoded this in `guion-style-ccc.md` for future episodes.
- Ran retrospective + skill audit: fixed a stale/broken reference in
  `episode-pipeline/workflows/03b-marketing.md` that pointed to a nonexistent
  `btq-project/workflows/quote-cards.md` — corrected to describe how BTQ/MPD/CCC actually
  each generate quote cards.
- Andy explicitly paused Stage C (marketing/social plan) — he's setting up CCC's social
  accounts and Spotify RSS distribution manually first. Logged as an intentional pause, not
  a blocker, so it isn't mistaken for unfinished work next session.

## Where We Paused

**Last action:** Fixed the stale quote-cards reference in `03b-marketing.md`, committed all
of this session's CCC + skill work (`c2b9470`).
**Next action:** Continuity sync (Step 4 of `/session-close`) — not yet run as of this file.
**Blockers:** None technical. Two items are explicitly waiting on Andy (not something to
chase proactively next session):
1. Generate the 6 quote-card images from `quotecards-ep001.md` in Flow/Nani Banana.
2. Regenerate the 1:1 cover art (higher res + correct parchment title fill) before
   Stage C.

## Files to Read First

- `corporate-crime-confidential-production/pipeline-state-ep001.md` — full status, all
  findings, and why Stage C is paused. This is the single source of truth for EP001.
- `corporate-crime-confidential-production/guion-style-ccc.md` — CCC's calibration doc,
  read before writing Case File No. 002's script.
- `podcast-creator/workflows/01-episodio.md` (Paso 4) — the new word-count verification
  step; apply it when Case File No. 002's script gets written.

## Notes / Gotchas

- **A parallel session was active** working on BTQ EP020 at the same time (uncommitted
  changes to `btq-production/pipeline-state-ep020.md` and a new
  `btq-production/pipeline-audit-ep020.md`) — deliberately excluded from this session's
  commit via scoped `git add`. Do not assume those files are stale; a different session
  owns them.
- CCC's actual quote-cards precedent (`quotecards-ep001.md`) is a genuinely new pattern —
  first CCC episode to need this, and the attribution rule (real quote vs. dramatized
  narration) doesn't apply to BTQ/MPD since they don't use composite characters or
  reconstructed first-person monologue.
- The podcast-creator skill's actual folder lives at
  `.claude/skills/podcast-creator/` (nested), not at the repo root — this matches
  `skill-management/SKILL.md`'s documented global-vs-project-specific split, it just isn't
  obvious from casual browsing.

## Questions to Answer

- None open from Andy's side — next session starts fresh with **Case File No. 002** once
  he shares the case info, per his own direction at episode close.
